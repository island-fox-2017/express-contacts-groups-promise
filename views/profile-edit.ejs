<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Edit Profile Page</title>
    <link href="/style.css" rel="stylesheet" type="text/css">
    <link rel="stylesheet" href="/font-awesome/css/font-awesome.min.css">
  </head>
  <body>
    <h1><center><i class="fa fa-pencil"> Edit Profile Contact</i></center></h1>
    <div>
      <form id="main-form" class="modal-content animate" name="mainForm" action="/profile/update/<%= data[0].id %>" method="post">
        <div class="container">
          <%= dataContact[0].long_name %>
          <label><b>ID</b></label>
          <input type="text" name="id" value="<%= data[0].id %>" disabled/>
          <label><b>Username</b></label>
          <input type="text" name="username" value="<%= data[0].username %>" />
          <label><b>Password</b></label>
          <input type="text" name="pwd" value="<%= data[0].pwd %>" />
          <label><b>ID Contact</b></label>
          <!-- <input type="text" name="contact_id" value="<%= data[0].contact_id %>" /> -->
          <select name="contact_id">
            <option value="<%= data[0].contact_id %>"><%= data[0].contact_id %></option>
            <% dataContact.forEach(d => { %>
              <option value="<%= d.id %>"><%= d.id %> <%= d.long_name %></option>
            <% }) %>
          </select>
          <div class="clearfix">
            <button id="cancelbtn" class="cancelbtn" onclick="window.location='/profile'">Cancel</button>
            <button type="submit" class="submitbtn">Submit</button>
          </div>
        </div>
      </form>
    </div>
  </body>
</html>
